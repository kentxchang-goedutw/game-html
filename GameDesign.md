這是一份為網頁遊戲《九九神殿探險隊》編寫的詳細設計文件 (GDD)。這份文件旨在提供清晰的結構、邏輯與數值設計，方便後續 AI 根據此內容直接撰寫代碼。

---

# 九九神殿探險隊 - 遊戲設計文件

## 1. 遊戲概述 (Overview) 與 學習目標對應
《九九神殿探險隊》是一款結合「逆向運算」與「因子分解」概念的數學益智網頁遊戲。玩家扮演一名進入古代數學神殿的探險家，必須透過將正確的「能量石」（乘數）填入「石門」（乘積）的凹槽中來解鎖通道。

### 學習目標對應：
*   **直覺建立**：透過 2, 5, 10 的倍數，讓學生快速進入遊戲節奏。
*   **邏輯推理**：理解 3, 4, 8 之間的翻倍關係（例如：看到 24，可以用 $3 \times 8$ 或 $4 \times 6$）。
*   **高階挑戰**：強化對 6, 7, 9 等難記數字的熟練度。
*   **反應訓練**：透過倒數計時與有限資源，訓練快速心算能力。

---

## 2. 核心機制 (Core Mechanics)

### A. 拼圖式乘法 (Inverse Multiplication)
*   遊戲的核心不是詢問「$3 \times 4$ 等於多少」，而是顯示「12」，要求玩家從背包中選出「3」與「4」放入。
*   **操作方式**：點擊或拖動下方「背包欄」中的數字石塊，放入石門上的兩個圓形凹槽。

### B. 背包系統與資源限制
*   玩家的背包有固定容量（例如每關提供 8-10 顆能量石）。
*   **高難度設計**：在進階關卡中，如果目標是 12，而玩家用掉了「2」和「6」，之後遇到 18 時，可能只剩下「3」可用。玩家必須思考最優組合。

### C. 關卡進度與勝負條件
*   **過關條件**：連續開啟 5 道石門即可通過一個大關。
*   **失敗條件**：
    1.  **生命值 (HP)**：放入錯誤組合，扣除一顆心。心碎完則遊戲結束。
    2.  **時間 (Timer)**：單一石門有倒計時，時間到未完成則扣除 HP 並強制更換題目。

### D. 數值設計 (Difficulty Scaling)
*   **Stage 1 (規律性)**：目標數為 10, 20, 15, 25, 50 等。
*   **Stage 2 (翻倍關係)**：目標數為 12, 16, 24, 32 等。
*   **Stage 3 (高難度)**：目標數為 42, 56, 63, 81 等。

---

## 3. 遊戲流程 (Game Loop / Flow)

1.  **開始介面 (Start Screen)**：
    *   遊戲標題、探險家角色插圖、「開始探險」按鈕、遊戲說明。
2.  **地圖選擇/自動進場**：
    *   由易到難進入：規律之林 (2/5/10) -> 翻倍迷宮 (3/4/8) -> 終極神殿 (6/7/9)。
3.  **戰鬥/解謎階段 (The Puzzle)**：
    *   角色走到門前 -> 顯示門上的數字 -> 玩家觀察背包石塊 -> 進行配對 -> 門開 (Success) 或 震動 (Fail)。
4.  **結算介面 (Result Screen)**：
    *   顯示該關卡耗時、正確率、獲得的寶藏勳章。
    *   「下一關」或「重新挑戰」。

---

## 4. 畫面佈局建議 (UI/UX Layout)

建議使用 **16:9** 的比例，並將所有內容整合在單一 HTML 檔案中：

*   **頂部狀態列 (Top Bar)**：
    *   左側：HP（心形圖示）。
    *   中間：當前關卡名稱與進度條。
    *   右側：剩餘時間（倒數計時條）。
*   **中央主視窗 (Main Area)**：
    *   背景：神殿內景。
    *   中央：巨大的石門，門上有一個大數字（乘積）與兩個空白凹槽（$ \bigcirc \times \bigcirc $）。
*   **底部背包區 (Inventory)**：
    *   一排圓形或方塊石塊，上面寫著數字（2-9）。
    *   設計為可點擊選取的狀態，選取後高亮顯示。
*   **控制區**：
    *   「清除」按鈕（重放石塊）。
    *   「確認」按鈕（有些設計可設為放入即自動檢查）。

---

## 5. 技術實作建議 (Technical Implementation)

*   **開發語言**：Vanilla JS (ES6+), HTML5, CSS3。不需要外部框架，確保輕量化。
*   **渲染方式**：
    *   推薦使用 **DOM 操作** 為主，因為這是一個典型的「物件點擊/拖放」遊戲，DOM 較易處理文字與動畫。
    *   或者使用 **Canvas API** 繪製角色移動動畫與場景變化。
*   **數據結構範例**：
    ```javascript
    const levels = [
      {
        id: 1,
        title: "規律之林",
        targets: [10, 25, 40, 5, 100], // 題目
        stones: [2, 5, 5, 8, 5, 1, 10, 10] // 背包裡的石頭
      },
      // ...
    ];
    ```
*   **碰撞與互動**：
    *   若使用拖放，利用 `dragstart`, `dragover`, `drop` 事件。
    *   若使用點擊，則用簡單的狀態機（`selectedStone` -> `targetSlot`）。
*   **動畫效果**：使用 CSS Transitions 或 Web Animations API 處理石門開啟與震動效果。

---

## 6. 學習目標映射清單

| 遊戲元素/機制 | 對應學習目標 |
| :--- | :--- |
| **Stage 1 的 5 與 10 的倍數題目** | 熟練掌握 2、5、10 等具有規律性的數字乘法 |
| **同時提供 24 作為目標，與 3, 4, 6, 8 石塊** | 掌握 3、4、8 倍數間的翻倍關係與計算規律 |
| **Stage 3 出現 49(7x7), 54(6x9), 56(7x8)** | 挑戰 6、7、9 等高難度數字的準確運算 |
| **單一門扉的倒計時條 (15秒減至5秒)** | 在限定時間內快速回答隨機出現的乘法題目 |
| **逆向給積求因子 (Target 12 -> 2,6 or 3,4)** | 深化對乘法表靈活運用與因數分解的基礎理解 |

---

這份設計文件現在可以交由 AI 助手，並下達指令：「**請根據這份設計文件，撰寫一個單一 HTML 檔案的網頁遊戲，包含所有邏輯、樣式與資源。**」